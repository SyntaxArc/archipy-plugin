## Summary

## Checklist

- [ ] `python3 scripts/check_catalog.py` passes
- [ ] `python3 -m unittest discover -s tests -v` passes
- [ ] `CHANGELOG.md` updated for user-facing changes
- [ ] All four JSON manifests share the same `version` (when releasing)
- [ ] Dual-IDE parity considered (Cursor + Claude Code)

## Test plan

-
