# Test-only settings for Behave. Container endpoints/credentials are patched in at runtime; set only what differs
# from the defaults in features/environment.py (TestConfig) and the app's required settings.
ENVIRONMENT=TEST
# TESTCONTAINERS_RYUK_CONTAINER_IMAGE=testcontainers/ryuk:0.14.0
# POSTGRES__IMAGE=postgres:18.6-alpine
# REDIS__IMAGE=redis:8.10.1-alpine
# KAFKA__IMAGE=apache/kafka:4.3.1
# TEMPORAL__IMAGE=temporalio/temporal:1.8.2
# TEMPORAL__TASK_QUEUE=test-task-queue
